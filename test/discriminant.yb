export def for[int] *> discriminated = {
	tag: ~,
	(
		let first: int = tag if 1,
		let extra: int = 1,
		;
		let second: int = tag if 2,
		extra: ~,
		let additional: int = extra + tag,
		;
		let third: int = tag if 3,
		additional: ~,
	)
}
