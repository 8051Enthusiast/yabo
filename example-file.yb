parser expr1 = for [u8] *> {
    (
    	let a: u64 = 3;
    |
	let a: u64 = 4;
    )
    (
        let b: u64 ~ 1 || 2 && 3 || 4 = a | 5 | 3;
    |
	let _: u64 = 0;
    )
    let x: u64 = b else a else 0 + a;
}
parser test = for [u8] *> {
	let a: u64 ~ 34 && 44 = 3;
    |
	let a: u64 = 2;
    |
	(
		let a: u64 = 3;
	|
		let a: u64 = 7 * 5 + a;
	)
}
