parser complex = for [u8] *> {
    a: u64;
    (
        c: u64;
        d: c_string |> utf8;
	e: u64;
	let h: str = "asdf";
	u64 ~ 0;
	f: {
		a: u64;
		b: u64;
		let return: u64 = 4;
		c: u64;
	};
	g: u64;
        |
        c: u64 ~ 1 || 2;
        let d: str = 3;
    )
    b: u64;
    let x: u64 = b else a else 0 + a;
    |
    a: u64;
    let m: u64 = e * m + 3 * 5 | 4;
}
