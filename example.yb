def for[int] *> main = {
	x: ~,
	y: ~,
		let f: int = z * x if 7,
	(
		z: ~,
		;
		let z: int = 3 + 4,
		g: ~,
		let e: int = z * y,
	)
	a: ~,
	;
	x: ~,
	m: {
		d: ~,
	},
	n: {},
	let g: int = m.d | x,
	a: ~,
}
