====[ source code ]====
fun ~last_byte: int = {
  case
  | if ~ is !eof
    return: if last_byte
  | return: if ~ is !eof
  \
}

export fun ~test = expect last_byte

====[ binary 1 ]====
01 02 03

====[ output 1 ]====
3
