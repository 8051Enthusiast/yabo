====[ source code ]====
def ~f(a: int, b: int) = {
    let return = a * b
}

export
def ~test = {
    let j = f(6, 7, 8, ..)
           #~^error[507] expected a function argument, but got no function argument, error introduced here
}
